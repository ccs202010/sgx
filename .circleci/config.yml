  
# Python CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-python/ for more details
#
version: 2
jobs:
  build:
    steps:
      - checkout
      # run tests!
      - run:
          name: run tests
          command: |
            chmod +777 ./sgx-init && chmod +777 config.json && ./sgx-init
